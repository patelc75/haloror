<h1>Editing activity</h1>

<%= error_messages_for :activity %>

<% form_for(:activity, :url => activity_path(@activity), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', activity_path(@activity) %> |
<%= link_to 'Back', activities_path %>