<h1>Listing falls</h1>

<table>
  <tr>
  </tr>
  
<% for fall in @falls %>
  <tr>
    <td><%= link_to 'Show', fall_path(fall) %></td>
    <td><%= link_to 'Edit', edit_fall_path(fall) %></td>
    <td><%= link_to 'Destroy', fall_path(fall), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New fall', new_fall_path %>