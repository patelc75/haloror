<h1>New heartrate</h1>

<%= error_messages_for :heartrate %>

<% form_for(:heartrate, :url => heartrates_path) do |f| %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', heartrates_path %>