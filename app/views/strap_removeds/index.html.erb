<h1>Listing strap_removeds</h1>

<table>
  <tr>
  </tr>

<% for strap_removed in @strap_removeds %>
  <tr>
    <td><%= link_to 'Show', strap_removed %></td>
    <td><%= link_to 'Edit', edit_strap_removed_path(strap_removed) %></td>
    <td><%= link_to 'Destroy', strap_removed, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New strap_removed', new_strap_removed_path %>
