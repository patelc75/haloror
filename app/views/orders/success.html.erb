<h1 id="thank_you_for_placing_the_order_with_us">Thank you for placing the order with us</h1>

<p>
  Your card has been charged <strong><%= number_to_currency(@order.cost, :precision => 2, :unit => '$') %></strong>. 
  Order confirmation has been emailed to <strong><%= @order.bill_email %></strong>
</p>
<hr/>
<table width="100%">
  <tr>
    <td>Order number: <strong><%= @order.full_number %></strong></td>
    <td>Order date: <%= @order.created_at.to_s(:day_date) %></td>
  </tr>
</table>

<br />

<table width="100%">
  <tr align="left">
    <th>Product</th>
    <th>Status</th>
    <th>Quantity</th>
    <th>Item price</th>
    <th>Total price</th>
  </tr>
  <% @order.order_items.each do |item| %>
  <tr align="left">
    <td><%= item.product_model %></td>
    <td><%= item.status %></td>
    <td><%= item.quantity %></td>
    <td><%= item.formatted_cost %></td>
    <td><%= item.formatted_cost(item.quantity) %></td>
  </tr>
  <% end -%>
</table>
<br>
<table width="100%">
  <tr align="left">
    <th>Billing address</th>
    <th><strong>Your payment</strong></th>
  </tr>
  <tr>
    <td>
      <%= @order.bill_first_name %> <%= @order.bill_last_name %><br />
      <%= @order.bill_address %><br />
      <%= @order.bill_city %>, <%= @order.bill_state %> <%= @order.bill_zip %><br />
      <%= @order.bill_phone %><br />
      <%= @order.bill_email %><br />
    </td>
    <td valign="top">
      Card: *****<%= @order.card_number[-4..-1] %><br />
      Expiry: <%= @order.card_expiry %>
    </td>
  </tr>
</table>
<br><br>
<fieldset style="border: 2px solid ; padding: 10px;">
<legend><b>Follow-up Forms and Instructions</b></legend>
Halo support will soon be calling you to begin installation. In the meantime, please print the following forms and fill them out. Installation cannot begin without them:
<%= render :partial => 'users/forms_and_installation'%>
</fieldset>
