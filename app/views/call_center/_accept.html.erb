<% if action = event.accepted? %>
	Accepted by <%=action.user.login%> at <%=action.created_at.to_s(:short)%>
<% else %>
	<%=link_to_remote "Accept", :update => "#{event.id}_accept", :url => { :action => "accept", :id => event.id }%>
<% end %>