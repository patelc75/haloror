<% if action = event.resolved? %>
	Resolved by <%=action.user.name%> at <%=UtilityHelper.format_datetime(action.created_at, current_user)%>
<% else %>
	<%=link_to_remote "Resolve", :update => "#{event.id}_resolve", :url => { :action => "resolve", :id => event.id }%>
<% end %>