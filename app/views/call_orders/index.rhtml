<h1>Listing call_orders</h1>

<table>
  <tr>
  </tr>
  
<% for call_order in @call_orders %>
  <tr>
    <td><%= link_to 'Show', call_order_path(call_order) %></td>
    <td><%= link_to 'Edit', edit_call_order_path(call_order) %></td>
    <td><%= link_to 'Destroy', call_order_path(call_order), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New call_order', new_call_order_path %>