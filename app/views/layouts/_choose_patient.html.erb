Senior:	
<select onchange="if(this.value != ''){document.location = '/<%=params[:controller]%>/<%=params[:action]%>/'+this.value}">
	<% if @user.is_halouser? %>
		<option value="<%=@user.id%>"><%=@user.name%></option>
	<% end %>
	<option value="">---</option>
	
	<% @user.patients.each do |patient| %>
		<option value="<%=patient.id%>" <%='selected' if params[:id].to_i == patient.id %>><%=patient.name%></option>
	<% end %>
</select>