Successful Logins Daily Report
Begin: <%=  @period.in_time_zone('America/Chicago')  %> CST
End  : <%= Time.now.in_time_zone('America/Chicago')  %> CST

Name |  # Logins  |  Last Login  |  Main Role
-----------------------------------------------------------
<% @users.sort_by { |k,v| v[:last_login]}.each do |key| -%> 
<%= key[1][:name] %> (<%= key[0] %>) | <%= key[1][:occurrences] %> logins | Last login: <%= key[1][:last_login].in_time_zone('America/Chicago') %> CST | <%= key[1][:main_role] %> 
<% end -%>
