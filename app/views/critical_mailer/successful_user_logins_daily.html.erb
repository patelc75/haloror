<%  end_time = Time.now
    begin_time = 1.day.ago(end_time)
    users = DailyReports.successful_user_logins(begin_time, end_time)  -%>
Successful Logins:
All times are in UTC timezone
	Starting Time: <%= begin_time %>
	Ending   Time: <%= end_time %>
Name    Number of Logins    Roles
---------------------------------------------------------
<% users.each do |user| -%> 
<%= user.name %>    <%= user[:logins]%>    <%=render :partial => 'reporting/table/user_roles', :locals => {:user => user} -%>
<% end -%>