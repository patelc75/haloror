Device Not Worn Daily Report
Begin: <%=  @period.in_time_zone('America/Chicago')  %> CST
End  : <%= Time.now.in_time_zone('America/Chicago')  %> CST

User  |  Occurrences  |  Total Duration
------------------------------------------------------------
<% @users.sort_by { |k,v| v[:duration]}.each do |key| -%> 
<%= key[1][:name] %> (<%= key[0] %>) | <%= key[1][:occurrences] %> intervals | <%= UtilityHelper.seconds_format(key[1][:duration]) %>
<% end -%>	

Go to My Links > User Compliance to get complete breakdown