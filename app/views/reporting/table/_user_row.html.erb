<% if toggle %>
	<a name="user_<%=user.id%>"></a>
<% end %>
<div class="row">
	<div class="col-25">
		<% if toggle %>
			<a href="javascript:void(0)" onClick="toggle_user_hidden(<%=user.id%>)" id="user_<%=user.id%>-toggle">[+]</a>
		<% else %>
			&nbsp;
		<% end %>
	</div>
	<div class="col-75">
		<%=user[:id]%>
	</div>
	<div class="col-200">
		
		<% if caregiver %>
			<a href="#user_<%=user.id%>">
		<% else %>
			
		<% end %>
		
		<% if profile = user.profile %>
			<%=profile[:first_name]%> <%=profile[:last_name]%>&nbsp;
		<% else %>	
			&nbsp;
		<% end %>
		
		<% if caregiver and user.profile %>
			</a>
			(<%=user.profile[:relationship]%>)
		<% end %>
	</div>
	<div class="col-100">
		<%=user[:login]%>&nbsp;
	</div>
	<div class="col-150">
		<%=render :partial => 'reporting/table/user_roles', :locals => {:user => user}%>
	</div>
	<div class="col-150">
		<% last_log = AccessLog.find(:first, :conditions => "user_id = #{user.id}", :order => 'created_at desc') %>
		<% if !last_log.blank? %>
			<a href="/logs/user/<%=user.id%>"><%= last_log.created_at.to_formatted_s(:short) %></a>
		<% else %>
			&nbsp;
		<% end %>
		
	</div>
	<% device = nil unless device %>
	<div class="col-200">
		<% if user.profile %>
			<%=link_to_remote_redbox('Profile', :url =>{ :controller => "profiles", :action => "edit_caregiver_profile", :id => user.profile[:id], :frame => true }, :html => {:method => :get})%> 
					<% if user.is_halouser? %>
							| <a href="/events/user/<%=user[:id]%>">Events</a> | <a href="/chart/flex/<%=user[:id]%>">Chart</a> 
							| <a href="/reporting/lost_data/<%=user[:id]%>">Lost&nbsp;Data</a> 
							| <%= link_to_remote "Start Range Test Only", :url => {:controller => 'installs', :action => 'start_range_test_only_init', :id => user.id} %>
							| <a href="<%= url_for(:controller => 'call_center', :action => 'all_user_notes', :id => user[:id], :user_id => user[:id])%>">Notes</a>			
			<% if device %> | <a href="/reporting/remove_user_mapping/<%=user.id%>?device_id=<%= device.id %>">Remove</a>
				<% end %>
					<% end %>
		<% else %>
			&nbsp;
		<% end %>
	</div>
	<!--
	<div class="col-50">
		<%# for session in user.self_test_sessions %>
			session: <%#= session.id %>
			<%# for step in session.self_test_steps %>
				<%#= step.self_test_step_description.description %> , 
			<%# end %>
			<br>
		<%# end %>
	</div>
	-->
</div>